

let attempt = 0; // initialization can be done outside the loop

while (attempt <= 4) { //loop will run untill value matches this condition

    console.log("Attempt :", attempt)
    attempt++; // Updation

}