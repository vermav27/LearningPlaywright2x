let fruits = []; // Empty
let freshFruits = ["apple", "mango", "banana"]; // Length = 3 , Index --> 0,1,2

let arr = [10, 20, 30, 40, 50]; // 0 to 4 Index
console.log(arr[3]);
console.log(arr[5]) // It will be undefined.

let testResult = ["pass", "fail", "pass", "skip"];
let mixed = ["hello", 23, true, null] // In javascript we can have mixture of different datatype.


