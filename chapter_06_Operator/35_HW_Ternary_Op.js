// Maximum between 2 Numbers
let a = 10;
let b = 20;
let result = (a > b) ? `${a} is maximum` : `${b} is maximum`;
console.log(result);

// Maximum between 3 Numbers
let x = 50;
let y = 35;
let z = 30;

let result2 = (x > y && x > z) ? `${x} is maximum` : (y > x && y > z) ? `${y} is maximum `:`${z} is maximum` ; 
console.log(result2);

